{
  "name": "bitman-justbuy-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npm run deploy:pages:main && npm run deploy:pages:production",
    "deploy:pages:main": "npx wrangler@latest pages deploy dist --project-name bitman-justbuy --branch main",
    "deploy:pages:production": "npx wrangler@latest pages deploy dist --project-name bitman-justbuy --branch production",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.13.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^25.3.5",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.4.1",
    "tailwindcss": "^4.0.0",
    "typescript": "~5.7.0",
    "vite": "^7.0.0",
    "vitest": "^4.1.10"
  }
}
